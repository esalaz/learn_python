#common use of tuples

coordinates = (4, 5)

#tuples are immutable, cannot be changed or modified
print(coordinates[0]);
print(coordinates[1]);

# a tuple is very simple you just create it and you have it.
# you could even create a list of tuples
birthdays = [("Erik", 8 , 21 , 1995)],["Maliyah", 3 , 10, 1997];

print(birthdays[0])
print(birthdays[1])
